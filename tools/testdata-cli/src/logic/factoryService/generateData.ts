//import { Factory, FactoryOptions } from '.';
import { createFactory } from '.';
import { Options } from '../options';

export const generateData = (options: Options, dst: string): number => {
  console.log(JSON.stringify(options));
  return 0;
}
