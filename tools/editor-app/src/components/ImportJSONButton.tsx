import React, { ButtonHTMLAttributes } from 'react';
import Button from '@mui/material/Button';

const ImportJSONButton = () => {
    return(
        <Button variant='contained'>Import JSON</Button>
    )   
}

export {ImportJSONButton}